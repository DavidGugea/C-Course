#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    const int FAV_NUM = 5;
    printf("%d\n", FAV_NUM);
    FAV_NUM = 8;
    printf("%d", FAV_NUM);
    */

    printf("Hello"); // "Hello is also a constant
    pritnf("%d", 90); // 90 is also considered a constant

    return 0;
}
